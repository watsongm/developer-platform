/**
 * Backstage MCP Backend Plugin
 *
 * Exposes the Backstage service catalog, TechDocs, and scaffolder as
 * Model Context Protocol (MCP) tools — making every Backstage instance
 * visible and actionable by AI coding agents.
 *
 * @packageDocumentation
 */

export { mcpPlugin as default, mcpPlugin } from './plugin';
